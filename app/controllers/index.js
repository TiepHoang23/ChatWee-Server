const userController = require('./users');
const authController = require('./auth');
const chatRoomController = require('./chatRoom');

module.exports = { userController, authController, chatRoomController };
